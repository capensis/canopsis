* **DELETE** ``/perfstore`` and ``/perfstore/:_id`` request body

.. code-block:: javascript

	[
		// list of ids to delete, ignored if URL's _id is set
	]

* **PUT** ``/perfstore`` request body

.. code-block:: javascript

	[
		{
			// See metric specification for more informations
		},
		// ...
	]
