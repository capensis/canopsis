# Custom DNS resolvers for Nginx.
#
# This file is empty on default package installations, since we just
# use 127.0.0.1 by default.
#
# If your CPS_API_URL does use a domain name, though, you will need
# to tell Nginx to use your local resolver:
#
#resolver DNS_RESOLVER_IP_ADDRESS;
