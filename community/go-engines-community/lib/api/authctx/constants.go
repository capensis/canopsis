package authctx

const (
	// UserKey is the context name for user id.
	UserKey  = "user_id"
	Username = "username"
	// APIKey is the context name for user api key.
	APIKey = "api_key"
	// Roles is the context name for user's roles
	Roles = "roles"
	// Locale is the context name for user's preferred locale.
	Locale = "locale"
)
