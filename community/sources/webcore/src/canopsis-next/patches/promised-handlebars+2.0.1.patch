diff --git a/node_modules/promised-handlebars/index.js b/node_modules/promised-handlebars/index.js
index 10da487..cb4c783 100644
--- a/node_modules/promised-handlebars/index.js
+++ b/node_modules/promised-handlebars/index.js
@@ -77,6 +77,22 @@ function promisedHandlebars (Handlebars, options) {
     // Wrap the compiled function
   })
 
+  // Register precompiled templates (which was generated by `precompile` function) into `engine.partials` object by `id`
+  // and wrap it with `prepareAndResolveMarkers`
+  engine.registerPrecompiledTemplate = function registerPrecompiledTemplate(id, precompiled) {
+    if (!engine.partials) {
+      engine.partials = {};
+    }
+
+    // We need to run it by `new Function` to convert string to JavaScript code
+    const precompiledFunc = new Function(`return ${precompiled}`);
+    const compileTemplate = engine.template(precompiledFunc());
+
+    engine.partials[id] = wrap(compileTemplate.bind(engine), prepareAndResolveMarkers);
+
+    return engine.partials[id];
+  };
+
   /**
    * Wrapper for templates, partials and block-helper callbacks
    *
