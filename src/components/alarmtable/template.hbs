<div class="box-body">
    <div class="table-responsive">
        <table class="table no-margin">
            <thead>
                <tr>
                    {{#each field in fields}}
                        {{#if field.isSortable}}
                            <th {{action 'click' field}}>{{field.name}} <span {{bind-attr class=":glyphicon field.isASC:glyphicon-sort-by-alphabet:glyphicon-sort-by-alphabet-alt"}} aria-hidden="true"></span></th>
                        {{else}}
                            <th {{action 'click' field}}>{{field.name}}</th>                    
                        {{/if}}
                    {{/each}}
                </tr>
            </thead>
            <tbody>
                {{#each alarm in sAlarms}}
                
                    {{#component-alarmraw alarm=alarm fields=controller.fields}}
                    {{/component-alarmraw}}

                {{/each}}
            </tbody>
        </table>
    </div>
</div>
