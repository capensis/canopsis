stages:
  - prebuild
  - build
  - test
  - packaging
  - publish

prebuild:
  stage: prebuild
  tags: ["canopsis"]
  only:
    - ci/new_ci
  script:
    - uname -a
    - docker -v
    - git --no-pager show

build_docker_debian9:
  stage: build
  tags: ["canopsis"]
  only:
    - ci/new_ci
  before_script:

  script:
    - make docker_images DISTRIBUTIONS=debian9

#  after_script: 
#    - make clean_images DISTRIBUTIONS=debian9

publish:
  stage: publish
  tags: ["canopsis"]

  before_script:
    - echo "publish ..."
#    - echo "$REGISTRY_PASSWORD" | docker login -u "$REGISTRY_USER" git.canopsis.net --password-stdin
    - docker login -u $REGISTRY_USER -p $REGISTRY_PASSWORD git.canopsis.net

  script:
#    - make docker_push DISTRIBUTIONS=debian9
    - echo "push..."
#
#  after_script:
#    - make clean_images DISTRIBUTIONS=debian9
