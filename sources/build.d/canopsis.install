NAME="canopsis"

LOG="$LOG_PATH/$NAME.log"
rm -f $LOG &> /dev/null

function build(){
    true
}

function install(){
    cd $SRC_PATH/$NAME
    fab -H localhost -u canopsis -p canopsis setup 1>> $LOG 2>> $LOG
}
