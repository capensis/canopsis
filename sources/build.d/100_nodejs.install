NAME="nodejs"

function install() {
    BASE="node-v$VERSION-$ARCH"
    if [ ! -f ${PREFIX}/bin/node ]; then
        if [ ! "$(${PREFIX}/bin/node -v 2> /dev/null)" = "v${VERSION}" ]; then
            tar xf externals/$BASE.tar.xz --strip-components 1 -C $PREFIX
            check_code $?
        fi
    fi
}

function update() {
    install
}
