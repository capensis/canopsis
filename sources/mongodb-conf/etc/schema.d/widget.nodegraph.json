{
  "type": "object",
  "metadata": {
      "icon": "list-alt"
  },
  "categories": [{
      "title": "General",
      "keys":["title", "datatype", "graph_id", "library"]
    }, {
      "title": "Options",
      "keys":["refreshableWidget", "refreshInterval", "graphid"]
    }],
  "properties": {
    "datatype": {
      "type": "string",
      "enum": ["graph", "topology"],
      "default": "topology",
      "role": "simplelist"
    },
    "graph_id": {
      "type": "string",
      "default": "test"
    },
    "refreshableWidget": {
      "type": "boolean",
      "default": true
    },
    "library": {
      "type": "string",
      "enum": ["d3", "jsplumb", "cytoscape"],
      "default": "d3",
      "role": "simplelist"
    }
  }
}
