{
	"type": "object",
	"categories": [{
		"title": "General",
		"keys":["stackableColumnsPriority"]
	}],

	"properties": {
		"stackableColumnsPriority": {
			"reorderable": true,
			"description": "Sorted list of columns to stack when there is not enough space to display every shown column",
			"type": "array"
		}
	}
}
