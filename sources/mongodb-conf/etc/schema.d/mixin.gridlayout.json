{
	"type": "object",
	"categories": [{
		"title": "General",
		"keys":["columnXS", "columnMD", "columnLG", "offset", "forcelegacy"]
	}],

	"properties": {
		"forcelegacy": {
			"type": "boolean",
			"default": true,
			"description": "force children widgets to use parent placement values",
			"title": "force legacy position"
		},
		"columnXS": {
			"type": "integer",
			"max": 12,
			"tick": 1,
			"default": 12,
			"role": "slider",
			"description": "Width of the widget in columns unit  for small definition screens"
		},
		"columnMD": {
			"type": "integer",
			"max": 12,
			"tick": 1,
			"default": 6,
			"role": "slider",
			"description": "Width of the widget in columns unit for medium definition screens"
		},
		"columnLG": {
			"type": "integer",
			"max": 12,
			"tick": 1,
			"default": 3,
			"role": "slider",
			"description": "Width of the widget in columns unit for large definition screens"
		},
		"offset": {
			"type": "integer",
			"max": 12,
			"tick": 1,
			"role": "slider",
			"description": "Columns number to skip before placing the widget"
		}
	}
}
