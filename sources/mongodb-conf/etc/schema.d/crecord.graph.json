{
    "type": "object",
    "categories": [{
        "title": "General",
        "keys":["name", "description", "enable", "nnodes", "nedges"]
    },{
        "title": "Filter",
        "keys":["mfilter"]
    }],
    "properties": {
        "crecord_type": {
            "enum": ["filter"],
            "required": true
        },
        "name": {
            "type": "string",
            "required": true,
            "default": "no name"
        },
        "description": {
            "type": "string"
        },
        "mfilter": {
            "type": "string",
            "required": true,
            "role": "cfilter"
        },
        "nnodes": {
            "type": "integer",
            "required": true,
            "default": 50
        },
        "nedges": {
            "type": "integer",
            "required": true,
            "default": 50
        },
        "type": {
            "enum": ["jsplumb", "d3", "cytoscape"],
            "default": "d3",
            "role": "simplelist"
        }
    }
}
