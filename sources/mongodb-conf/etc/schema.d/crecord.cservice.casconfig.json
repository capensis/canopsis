{
    "type": "object",
    "categories": [{
        "title": "General",
        "keys": ["title", "server", "service"]
    }],
    "properties": {
        "title": {
            "type": "string",
            "required": true,
            "description": "Login with CAS"
        },
        "server": {
            "type": "string",
            "required": true
        },

        "service": {
            "type": "string",
            "required": true,
            "default": "http://localhost:8082/"
        }
    }
}