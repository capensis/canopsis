{
	"type": "object",
	"categories": [{
		"title": "General",
		"keys": ["crecord_type", "uid", "source", "dtstart", "dtend", "freq", "duration", "ical"]
	}],
    "properties": {
        "uid": {
            "type": "string",
            "required": false,
            "description": "Unique id."
        },
        "crecord_type": {
            "enum": ["vevent"],
            "required": true
        },
        "source": {
        	"type": "string",
        	"required": true,
        	"description": "publishing entity id."
        },
        "dtstart": {
            "type": "number",
            "required": false,
            "role": "timestamp",
            "default": 0,
            "description": "Start timestamp."
        },
        "dtend": {
            "type": "number",
            "required": false,
            "role": "timestamp",
            "default": 2147483648,
            "description": "End timestamp."
        },
        "freq": {
            "type": "string",
            "required": false,
            "default": "",
            "description": "Recurrent rule."
        },
        "duration": {
            "type": "number",
            "required": false,
            "default": "",
            "description": "Period duration in seconds."
        },
        "iCal": {
            "type": "string",
            "required": false,
            "default": "",
            "description": "iCal format."
        }
    }
}
