{
    "type": "object",
    "metadata": {
        "classes": ["system"]
    },
    "categories": [
        {
            "title": "General",
            "keys": ["title", "graph_type", "graph_id", "layout"]
        }, {
            "title": "Options",
            "keys": ["refreshableWidget", "refreshInterval"]
        }
    ],
    "properties": {
        "crecord_type": {
            "type": "string",
            "required": true,
            "default": "wgraph"
        },
        "graph_type": {
            "type": "string",
            "enum": ["graph", "topology"],
            "default": "graph",
            "role": "simplelist"
        },
        "graph_id": {
            "type": "string"
        },
        "layout": {
            "type": "string",
            "enum": ["natural", "tree", "map"],
            "default": "natural",
            "role": "simplelist",
            "description": "graph layout"
        }
    }
}
